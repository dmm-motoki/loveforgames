<div class="container">
  <div class="row">
    <div class="col-md-3 bg-white text-center mt-5 mb-4 ml-4 py-2">
      <h2>会員登録</h2>
    </div>
  </div>
  <div class="row">
    <div class="col-md-6 bg-white">
      <div class="form-group m-3">
        <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
          <%= render "users/shared/error_messages", resource: resource %>
          <div class="field">
            <%= f.label :name, "ニックネーム", class: "font-weight-bold" %><br />
            <%= f.text_field :name, class: "form-control border-dark" %>
          </div>
          <div class="field">
            <%= f.label :email, "メールアドレス", class: "font-weight-bold" %><br />
            <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control border-dark" %>
          </div>
          <div class="field form-row">
            <div class="col-md-7">
              <%= f.label :password, "パスワード", class: "font-weight-bold" %>
              <% if @minimum_password_length %>
              <em class="font-weight-bold">(<%= @minimum_password_length %> 文字以上)</em>
              <% end %><br />
              <%= f.password_field :password, autocomplete: "new-password", class: "form-control border-dark" %>
            </div>
          </div>

          <div class="field form-row mb-3">
            <div class="col-md-7">
              <%= f.label :password_confirmation, "パスワード確認", class: "font-weight-bold" %><br />
              <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "form-control border-dark" %>
            </div>
          </div>

          <div class="actions">
            <%= f.submit "登 録", class: "btn btn-success px-4" %>
          </div>
        <% end %>
      </div>
      <p>アカウントをお持ちの方は<%= link_to "こちら", new_user_session_path %>から。</p>
    </div>
  </div>
</div>
