<div class="container">
  <div class="row">
    <div class="col-md-3 mt-5 mb-3">
      <h2 class="bg-light text-center py-3 font-weight-bold">ログイン</h2>
    </div>
  </div>

  <div class="row">
    <div class="col-md-5 bg-light">
      <div class="form-group m-3">
        <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
          <div class="field mb-2">
            <%= f.label :name, "ニックネーム", class: "font-weight-bold" %><br />
            <%= f.text_field :name, autofocus: true, class: "form-control border-dark" %>
          </div>

          <div class="field mb-4">
            <%= f.label :password, "パスワード", class: "font-weight-bold" %><br />
            <%= f.password_field :password, autocomplete: "current-password", class: "form-control border-dark" %>
          </div>

          <div class="actions">
            <%= f.submit "ログインする", class: "btn btn-success" %>
          </div>
        <% end %>
      </div>
      <p>アカウントをお持ちでない方は<%= link_to "こちら", new_user_registration_path %>から。</p>
    </div>
  </div>
</div>
